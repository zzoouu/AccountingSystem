import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
		flex: 1,
	},
	member: {
		backgroundColor: '#fff',
		height: 40,
		flex: 0,
		flexDirection: 'row',
		alignItems: 'center',
		paddingLeft: 10
	}
})
export default styles

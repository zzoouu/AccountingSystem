import baseUrl from '../router'
const billRouter = {
	bills: `${baseUrl}/bills`,
	createBill: `${baseUrl}/bills/createBill`,
	editBill: `${baseUrl}/bill/editBill`,
	bill: `${baseUrl}/bill`,
}
export default billRouter

